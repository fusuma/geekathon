{
  "name": "@repo/api",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "tsc",
    "dev": "pnpm build && node dev-server.js",
    "dev:watch": "tsc --watch",
    "dev:sam": "sam local start-api",
    "deploy": "sam build && sam deploy",
    "lint": "eslint \"**/*.ts\"",
    "test": "jest"
  },
  "dependencies": {
    "@repo/shared": "workspace:*",
    "@aws-sdk/client-dynamodb": "^3.445.0",
    "@aws-sdk/lib-dynamodb": "^3.445.0",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@repo/eslint-config": "workspace:*",
    "@repo/typescript-config": "workspace:*",
    "@types/aws-lambda": "^8.10.131",
    "@types/node": "^20.10.0",
    "cors": "^2.8.5",
    "eslint": "^8.57.0",
    "express": "^4.18.2",
    "jest": "^29.7.0",
    "typescript": "^5.3.0"
  }
}